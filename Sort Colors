public void sortColors(int[] nums) {
        
       for(int i=0;i<nums.length-1;i++){
                if(nums[i]>nums[i+1]){
                    int sort=nums[i];
                    nums[i]=nums[i+1];
                    nums[i+1]=sort;
                    i=-1;
                }
        }
